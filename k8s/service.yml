apiVersion: v1
kind: Service
metadata:
  name: vfdjvdf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vfdjvdf"
    port: 8080
  selector:
    app: vfdjvdf
